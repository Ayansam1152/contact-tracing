<aura:component implements="flexipage:availableForAllPageTypes">
    <aura:attribute name="label" type="String"/>
    <aura:attribute name="open" type="Boolean" default="false"/>

    <!--Register the block click event-->
    <aura:registerEvent name="onClick" type="c:BlockClickEvent"/>

    <div class="board-block cBlock slds-align--absolute-center slds-var-m-around_medium slds-var-p-around--medium" onclick="{!c.blockClickHandler}">
        
        <aura:if isTrue="{!v.open}">
            <div>
                {!v.label}
            </div>
        </aura:if>
    
    </div>

</aura:component>	
