<aura:component implements="flexipage:availableForAllPageTypes">

    <aura:attribute name="modes" type="List" default="[
    {'label': 'Easy', 'value': 'easy'},
    {'label': 'Medium', 'value': 'medium'},
    {'label': 'Hard', 'value': 'hard'},
    ]"/>

    <aura:attribute name="selectedGameMode" type="String"></aura:attribute>

    <lightning:card title="Word Shuffle">

    <lightning:layout horizontalAlign="center" multipleRows="true">
        <lightning:layoutItem  padding="around-small">
            <lightning:combobox 
                name="gameMode" 
                label="Game Mode" 
                variant="label-hidden"
                value="inProgress" 
                aura:id = "gameMode"
                options ="{!v.modes}" 
                placeholder="Select Game Mode"
            />
        </lightning:layoutItem>

        <lightning:layoutItem padding="around-small">
            <lightning:button variant="brand" label="Start New Game" title="Start New Game" onclick="{!c.startGame}" />
        </lightning:layoutItem>

        <lightning:layoutItem padding="around-small">
            <lightning:button label="ReShuffle" title="ReShuffle" onclick="{!c.reShuffleBoard}"/>
        </lightning:layoutItem>

        <aura:if isTrue="{! (v.selectedGameMode) != null}">
            <lightning:layoutItem  size="12">
                <c:Board mode="{!v.selectedGameMode}" aura:id = "boardComp"></c:Board>
            </lightning:layoutItem>
        </aura:if>
    </lightning:layout>
    </lightning:card>
</aura:component>	
